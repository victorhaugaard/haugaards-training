import { addTranslations } from './core'
import { UI } from './ui'
import { CARD_TEXT } from './cardsText'
import { CHEER_TEXT } from './cheers'

addTranslations(UI)
addTranslations(CARD_TEXT)
addTranslations(CHEER_TEXT)
